import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DaftarbelumbayarPage } from './daftarbelumbayar.page';

describe('DaftarbelumbayarPage', () => {
  let component: DaftarbelumbayarPage;
  let fixture: ComponentFixture<DaftarbelumbayarPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DaftarbelumbayarPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
