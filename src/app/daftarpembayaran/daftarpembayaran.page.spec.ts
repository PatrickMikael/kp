import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DaftarpembayaranPage } from './daftarpembayaran.page';

describe('DaftarpembayaranPage', () => {
  let component: DaftarpembayaranPage;
  let fixture: ComponentFixture<DaftarpembayaranPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DaftarpembayaranPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
